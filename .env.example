# Copy to `.env` for local testing with `npx netlify dev` (never commit .env).
# On Netlify, set these in: Site configuration → Environment variables.
GMAIL_USER=dev.roshni33@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password
CONTACT_TO=riyad.cse27@gmail.com
